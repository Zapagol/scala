package org.zap.scala

/**
  * - Monads is a structure that represents sequential computations, Monad is not a class or a trait; monad is a
  * concept.
  * - Monad is an Object that wraps another object in Scala.
  * - Monads are containers. That means they contain some sort of elements. Instead of allowing us to operate on
  * these elements directly, the container itself has certain properties.
  *   examples : Option[T], flatmap, map and etc..
  */
object Monads extends App{

}
